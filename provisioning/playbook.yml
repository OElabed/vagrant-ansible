---
- hosts: all
  become: yes

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600

  roles:
    - angstwad.docker_ubuntu
    - commandline_tools
    - devops_tools
    - develop_tools
    - os_config_tools